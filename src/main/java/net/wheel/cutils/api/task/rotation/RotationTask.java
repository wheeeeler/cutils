package net.wheel.cutils.api.task.rotation;

import net.wheel.cutils.api.task.basic.BasicTask;

public final class RotationTask extends BasicTask {

    public RotationTask(String name, int priority) {
        super(name, priority);
    }
}
